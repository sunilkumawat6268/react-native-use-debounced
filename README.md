# react-native-use-debounced

Tiny TypeScript hook to get a debounced value. Works in React and React Native.

## Install

npm:
```bash
npm install react-native-use-debounced

```# react-native-use-debounced
# react-native-use-debounced
